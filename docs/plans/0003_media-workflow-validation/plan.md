# 0003: メディア操作ワークフロー検証

作成日: 2026-08-23

更新日: 2026-09-05

状態: 進行中（基本確認は0019の最小CLI導入後に再開。選別・現像は必要時に確認）

## 目的

RAW+JPEG、Live Photo、通常動画をCLIで配置し、MacとSMB上のHDDで開けることを確認する。選別・現像は通常の取り込みに必須とせず、使うときに必要な操作だけ確認する。

## 方針変更

[0021](../0021_simplify-photo-workflow/plan.md) により、Macは `PhotoWork/`、HDDは `photo-library/` の各1ルートとし、年月・機器別の構成に統一する。イベントフォルダと保存確定操作は廃止する。過去の実施記録は当時のパスのまま保持し、新構成での検証済みとは扱わない。

## 対象範囲と前提

- 0019のCLIで、MacまたはSMB上のHDDへ少数のサンプルを配置する。両ルート以下は `YYYY/YYYY-MM/camera/` または `smartphone/` とする。
- 実機試験は既存データと衝突しない試験先で行う。試験用の隔離は検証時だけのもので、日常のHDD構成に作業領域を追加するものではない。
- iPhoneの取得はイメージキャプチャを使い、その後の配置をCLIで行う。
- SD・iPhone・Google Photos等のコピー元は保持する。削除試験は今回のテストコピーだけで行う。
- 各形式1〜2組を目安に、形式・操作・例外を代表するサンプルを使う。一律10組の件数条件は設けない。未確認の形式は推測で合格にしない。
- 両経路の通し確認は0006、Amazonは0004、Immichは0009で扱う。選別・現像項目の完了を後続の前提にしない。

## 実施状況（変更前の履歴）

### 2026-08-25

- Sony SDカードのルート直下にあったフォルダを、`PhotoInbox/current/sony-sd-2026-08-25/` へコピーした。画像・動画ファイルだけを選別せず、`DCIM/` と `PRIVATE/` を含むカード内のフォルダ構成を維持した。
- `DCIM/` と `PRIVATE/`、ARW、JPEG、動画ファイルの存在を確認した。JPEGとARWはFinderのQuick Lookで、動画はQuickTime Playerで開けた。
- iPhoneからの画像・動画をMacへコピーした。HEICは8件、MOVは2件である。同名のHEIC/MOVは1組確認できた。もう1件のMOVに同名のHEICはなく、通常動画である可能性がある。HEICはFinderのQuick Lookで、MOVはQuickTime Playerで開けた。
- 削除操作の検証候補として、Sonyの `DSC00240.JPG` と `DSC00240.ARW`、iPhoneの `IMG_1380.HEIC` と `IMG_1380.MOV` を選んだ。まだ削除していない。
- 上記4ファイルをMac上のテストコピーからゴミ箱へ移した。各元フォルダに対応ファイルが残っておらず、ゴミ箱に4ファイルがあることを確認した。SDカードとiPhone上のコピー元には変更を加えていない。
- darktableを起動し、`DSC00241.ARW` を現像してJPEGを書き出せた。現像済みJPEGはRAWと同じフォルダに `DSC00241_edit.JPG` として出力した。`DSC00241.ARW.xmp` がRAWと同じフォルダにあることを確認した。出力JPEGは開け、ピクセル寸法は `4330 × 6494`、カラープロファイルはsRGBである。
- 今回のテストコピーは通常撮影として、`ReadyForArchive/2026/2026-08/` に分類することを決めた。

## 基本確認の残作業

以下を `validation.md` に記録する。既存の結果とCLIのログを再利用し、全件の手書き転記は行わない。

- [ ] CLIで `DCIM/` 内のARW・JPEGと `PRIVATE/` 内の動画をMacとHDDへ配置し、同じ相対構成になることと閲覧・再生を確認する。
- [ ] iPhoneの原本取得方法を補足し、HEIC・Live Photo・通常動画の代表例を取得・配置して開けることを確認する。
- [ ] 最小版では明示年月を使い、自動分類の追加後は複数月の入力と日時不明の扱いを確認する。RAW/JPEG/XMPやHEIC/MOVの配置が分断されないことも確認する。
- [ ] 実行結果と代表形式の確認結果、未確認事項を記録する。衝突・切断・再実行の実装試験は0019の結果を参照し、同じ試験を重複して行わない。

## 必要時の選別・現像確認

日常運用で選別・現像を使うときに確認する。基本確認が済んだ後も、未使用の操作は保留としてよい。

- [ ] 不採用のRAW/JPEG/XMPまたはHEIC/MOVの組を特定し、テストコピーだけ削除できることを確認する。既存の削除結果を再利用する。
- [ ] RAWからフル解像度・sRGBの `_edit` 付きJPEGを同じ `camera/` に出力し、XMPがRAWの隣にあることを確認する。
- [ ] HDD保存後の現像を試す。SMBでの操作が不便なら必要分をMacへコピーし、同じ相対構成で扱えることを確認する。
- [ ] 既存XMPや現像済みJPEGを更新してHDDへ戻す場合は、対象を指定した明示的な更新手順と、第2 HDDへ更新を保存する手順を0007へ引き継ぐ。通常の取り込みで無断上書きしない。

## 検証項目

| ID | 区分 | 確認すること |
|---|---|---|
| M01 | 基本 | SDの静止画・動画をCLIで取り込み、開ける |
| M02 | 基本 | iPhoneのHEIC・Live Photo・通常動画を原本として取得・配置できる |
| M03 | 任意 | 必要なときだけ選別できる |
| M04 | 任意 | 不採用のRAW/JPEG/XMPのテストコピーを削除できる |
| M05 | 任意 | Live PhotoのHEIC/MOVのテストコピーを削除できる |
| M06 | 任意 | 保存後もRAWを現像し、XMPを保存できる |
| M07 | 任意 | 現像済みJPEGをRAWと同じ `camera/` に追加できる |
| M08 | 基本 | 年月・機器別の配置と、判定できないファイルの報告を確認できる |
| M09 | 基本 | ログと代表例の結果・未確認事項を残せる |

## 完了条件

- 基本確認の対象形式・配置・閲覧の結果を `validation.md` に記録している。未確認形式はその形式の運用判断を保留している。
- 任意の選別・現像は、確認済みまたは未使用のため保留と明記している。全任意項目の実施をプラン完了の条件にしない。
- コピー元・本データを削除していない。
- 0004・0006・0007へ渡す結果と未解決事項が明確である。
